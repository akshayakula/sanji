{
  "name": "sanji-local-server",
  "version": "1.0.0",
  "description": "Local Node server controlled via ngrok, uses Playwright to control browser on this Mac",
  "main": "server.js",
  "scripts": {
    "start": "node start.js",
    "start:server": "node server.js",
    "dev": "CDP_ENDPOINT=http://127.0.0.1:9222 nodemon server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
