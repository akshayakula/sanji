# Local server (sanji local-server) — always runs on 8080
PORT=8080

# Ngrok URL — use localhost for local dev or your ngrok URL (e.g. from Netlify env)
# Examples:
#   Local:   http://localhost:8080
#   Ngrok:   https://abc123.ngrok.io
NGROK_URL=https://willing-accurately-albacore.ngrok-free.app/
OPENAI_API_KEY=

# Optional: control your already-open Chrome instead of launching a new one.
# Start Chrome with: --remote-debugging-port=9222 then set:
# CDP_ENDPOINT=http://localhost:9222
VAPI_API_KEY=